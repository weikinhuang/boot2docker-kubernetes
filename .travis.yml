sudo: required

language: bash

services:
  - docker

script:
  - docker-compose build
  - docker-compose up
  - sh -c 'docker-compose exec master journalctl -f -u bootkube.service | { sed "/Started Bootkube Kubernetes Bootstrap/ q" && kill $$ ;}'
  - ./test/test-ready.sh
